# Basic configuration for the service
# Add your specific configuration details here

service:
  name: Catalyst
  version: 1.0.0
  description: Configuration for the Catalyst service

billing-payments-service:
  name: billing-payments-service
  version: 1.0.0
  description: Configuration for the billing-payments-service service

auth-rbac-service:
  name: auth-rbac-service
  version: 1.0.0
  description: Configuration for the auth-rbac-service service

# Add more configuration options as needed for each service 